'use client';

export default function ChatContent({
  id,
  initialMessages,
  selectedModelId,
}: {
  id: string;
  initialMessages: Message[];
  selectedModelId: string;
}) {
  // ... your existing Chat component logic
  
  return (
    // ... your existing Chat component JSX
  );
} 